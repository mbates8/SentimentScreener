# .env.example
# Rename this file to .env and fill in the real values on your machine.
# DO NOT commit the real .env file to GitHub.

# Database settings
DB_HOST=localhost
DB_PORT=3306
DB_NAME=stock_data
DB_USER=sentiment_user
DB_PASSWORD=your_db_password_here

# OR: If you prefer a single canonical DATABASE_URL, use one of these instead
# (uncomment only one style; if you use DATABASE_URL the DB_* values are optional)
# Example SQLAlchemy URL (note: password must be URL-encoded if it has special chars)
# DATABASE_URL=mysql+mysqlconnector://sentiment_user:your_password%21@localhost:3306/stock_data

# App settings
POLL_INTERVAL_MS=60000
DEMO_MAX_SYMBOLS=200
DB_READ_TIMEOUT_S=30
DATA_SOURCE=demo   # set to "demo" for local demo mode, "live" to use the DB

# Finviz (optional) - some scrapers require a cookie key for the Elite export
FINVIZ_ASPXAUTH=your_finviz_aspxauth_cookie_here

# OpenAI (optional) - required only if you will run the GPT scoring script
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Other optional creds
# FINVIZ_ASPXAUTH may be required for the CSV exporter if you used Finviz Elite
# Add any other environment variables your local run requires below